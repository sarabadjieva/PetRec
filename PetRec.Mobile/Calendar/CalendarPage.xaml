<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PetRec.Mobile.Calendar.CalendarPage"
             xmlns:calendar="clr-namespace:PetRec.Mobile.Calendar"
             Title="CalendarPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

            <CollectionView x:Name="CalendarListView"
                            SemanticProperties.Hint="List of calendar entries for this pet"
                            ItemsSource="{Binding CalendarEntries}">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="calendar:CalendarEntryViewModel">
                        <HorizontalStackLayout HorizontalOptions="Start"
                                               VerticalOptions="Center"
                                               BackgroundColor="DarkSlateGrey"
                                               HeightRequest="75">
                            <Label Text="{Binding DateAndTime}" HorizontalTextAlignment="Center"/>
                            <Label Text="{Binding Id}" HorizontalTextAlignment="Center"/>
                            <Label Text="{Binding Description}" HorizontalTextAlignment="Center"/>
                        </HorizontalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Button
                x:Name="AddEntryBtn"
                Text="Add entry" 
                SemanticProperties.Hint="Adds entry to registry"
                Command="{Binding AddEntryCommand}"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>